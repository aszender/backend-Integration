# Backend environment example
PORT=5001
MONGODB_URI=mongodb://appuser:apppassword@localhost:27017/userdb

# JWT
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=1h

# If true, protects /api/users with JWT (otherwise the demo UI works without auth)
ENFORCE_AUTH=false
